[project]
name = "protected-branch-ruleset-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
